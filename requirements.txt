# Core -- necessário para jogar (modo terminal e modos de navegador)
pandas>=2.0
playwright>=1.40
opencv-python>=4.8
numpy>=1.24
joblib>=1.3

# Deep learning -- só necessário para a estratégia PlayOnWebsiteDeepLearning
# (compvision.py carrega isso sob demanda; sem tensorflow, as outras
# estratégias continuam funcionando normalmente)
tensorflow>=2.15

# Análise exploratória -- só necessário para eda_datasets.ipynb
matplotlib>=3.7
seaborn>=0.13
